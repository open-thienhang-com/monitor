package systems

import "mono.thienhang.com/pkg/context"

func (s *System) prometheusHandler(ctx *context.Context) {
	// return

	// promhttp.Handler().ServeHTTP(&ctx.Response, ctx.Request)
	// ctx.SetHandlers(promhttp.Handler().)
}
